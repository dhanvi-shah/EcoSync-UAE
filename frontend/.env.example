# Optional — only if the API is not at http://localhost:8000 (e.g. deployed backend).
# Copy to .env.local — Vite exposes only variables prefixed with VITE_.
# Never put secrets in VITE_* vars (they are embedded in the browser bundle).

# VITE_API_BASE_URL=https://your-api.example.com
